# Dependencies
node_modules/
.pnp
.pnp.js

# Next.js
.next/
out/
build/

# Production / standalone (локальная сборка)
dist/
dist-ssr/

# Environment (шаблон .env.example коммитим)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.*.local
!.env.example

# Railway / Vercel (локальные метаданные деплоя)
.vercel/

# TypeScript
*.tsbuildinfo

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Test coverage
coverage/

# PostgreSQL / дампы для миграции (scripts/migrate-from-supabase.mjs)
dump/
*.dump
*.sql.gz

# Supabase CLI (архив migrations в repo — не игнорируем; только локальный кэш CLI)
supabase/.branches/
supabase/.temp/

# Docker (локальные override, не Dockerfile)
docker-compose.override.yml
.docker/

# IDE / редактор
.vscode/*
!.vscode/extensions.json
.idea/
*.swp
*.swo
.cursor/

# OS
.DS_Store
Thumbs.db
Desktop.ini
ehthumbs.db

# Draw.io / временные файлы редакторов
*.bkp
.~lock.*

# Архивы переноса проекта (не коммитить случайно)
*.zip
*.7z
*.rar
*.tar.gz

# TLS / ключи
*.pem
*.key
